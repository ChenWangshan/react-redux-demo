
import { call, put, takeLatest } from 'redux-saga/effects';

// import api from '../../api';

// import actionCreators from './actionCreators';
// import { GET_MAPINFO_SAGA } from './actionTypes';

function* get() {

}

export default function* saga() {
	// yield takeLatest(GET_MAPINFO_SAGA, get);
} 